import 'package:flutter/material.dart';

class CarcassIntakeScreen extends StatelessWidget {
  const CarcassIntakeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('CarcassIntakeScreen'));
}
