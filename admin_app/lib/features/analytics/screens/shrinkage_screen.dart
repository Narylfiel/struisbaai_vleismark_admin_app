import 'package:flutter/material.dart';

class ShrinkageScreen extends StatelessWidget {
  const ShrinkageScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('ShrinkageScreen'));
}
