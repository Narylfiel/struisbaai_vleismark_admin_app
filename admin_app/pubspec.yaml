name: admin_app
description: Struisbaai Vleismark - Admin & Back-Office Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.3

  # Database
  supabase_flutter: ^2.0.0

  # Connectivity (offline detection)
  connectivity_plus: ^6.0.0

  # Offline Cache (Production workflows)
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.1.1

  # Charts & Data Tables (28+ for Flutter 3.22+ compatibility)
  syncfusion_flutter_charts: ^28.0.0
  syncfusion_flutter_datagrid: ^28.0.0

  # PDF & Printing
  pdf: ^3.10.7
  printing: ^5.11.3

  # Excel & CSV
  excel: ^4.0.3
  csv: ^5.1.1

  # File Handling
  file_picker: ^6.1.1
  image_picker: ^1.0.7

  # Barcode scanning (stock take — Admin only)
  mobile_scanner: ^5.0.0
  # Barcode display (loyalty membership cards)
  barcode_widget: ^2.0.4

  # Security
  crypto: ^3.0.3

  # Formatting
  intl: ^0.18.1
  uuid: ^4.2.2

  # External Links
  url_launcher: ^6.2.4

  # Google APIs (OCR)
  googleapis: ^12.0.0
  http: ^1.2.0
  dio: ^5.4.0

  # Icons
  cupertino_icons: ^1.0.6
  flutter_colorpicker: ^1.1.0

  share_plus: any
  equatable: any
  shared_preferences: ^2.5.4
  mailer: ^6.1.2
  flutter_secure_storage: ^9.0.0
  google_sign_in: ^7.2.0
  googleapis_auth: ^2.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  isar_generator: ^3.1.0
  build_runner: ^2.4.8
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/secrets/google_service_account.json
    - assets/images/Admin_App.png

flutter_launcher_icons:
  image_path: "assets/images/Admin_App.png"
  android: true
  ios: false
  windows:
    generate: true
    icon_size: 256